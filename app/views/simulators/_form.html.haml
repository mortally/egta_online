= simple_form_for entry, :html => {:multipart => true} do |f|
  = f.input :name
  = f.input :version
  = f.input :description, :as => :text
  = f.input :simulator_source, :as => :file
  = f.button :submit